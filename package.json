{
  "name": "localsessionstorageapi",
  "version": "1.0.0",
  "description": "<p>localSessionStorageAPI is an API that simplifies the CRUD operations with the localStorage and sessionStorage</p>",
  "main": "index.js",
  "scripts": {
    "test:cypress-headless": "./node_modules/.bin/cypress run --headless",
    "test:cypress-headed": "./node_modules/.bin/cypress run --headed --browser electron --no-exit ",
    "test-jest": "jest --coverage"
  },
  "keywords": [
    "$"
  ],
  "author": "Josemagne",
  "license": "ISC",
  "devDependencies": {
    "@types/cypress": "^1.1.3",
    "@types/jest": "^27.0.3",
    "@types/mocha": "^9.0.0",
    "cypress": "^9.1.0",
    "eslint": "^8.3.0",
    "husky": "^7.0.4",
    "jest": "^27.3.1",
    "ts-jest": "^27.0.7",
    "typescript": "^4.5.2"
  }
}
